namespace TkOlympApp.Services
{
    public static class PlainTextService
    {
        // Holds temporary plain text to pass between pages when query navigation is unsuitable
        public static string? LastText { get; set; }
    }
}
