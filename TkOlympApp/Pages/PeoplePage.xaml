<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             x:Class="TkOlympApp.Pages.PeoplePage"
             xmlns:svc="clr-namespace:TkOlympApp.Services"
             Title="{svc:Translate People_Title}">

    <Grid RowDefinitions="Auto,*,Auto">
        <Grid Grid.Row="0" Padding="12" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
            <Label Grid.Column="0" Text="{svc:Translate People_SortLabel}" VerticalOptions="Center" />
            <Picker Grid.Column="1" x:Name="SortPicker" Title="{svc:Translate People_SortPickerTitle}" WidthRequest="200" SelectedIndexChanged="OnSortChanged" HorizontalOptions="Start">
                <Picker.Items>
                    <x:String>{svc:Translate People_Sort_Alphabetical}</x:String>
                    <x:String>{svc:Translate People_Sort_ByBirthdays}</x:String>
                </Picker.Items>
            </Picker>
            <Button Grid.Column="2" x:Name="FilterButton" Text="{svc:Translate People_FilterButton}" Clicked="OnFilterClicked" HorizontalOptions="End" />
        </Grid>

        <RefreshView x:Name="PeopleRefresh" Grid.Row="1" Refreshing="OnPeopleRefresh">
            <ScrollView>
                <VerticalStackLayout x:Name="PeopleStack" Padding="12,6" Spacing="6" />
            </ScrollView>
        </RefreshView>

        <!-- Manual reload removed — use pull-to-refresh -->
    </Grid>
</ContentPage>
